# (주)케이원 관리자페이지 (CI4) — 작업노트

> 이 폴더를 건드리기 전에 먼저 읽는 노트. 작성 2026-09-03.
> **정적 홈페이지는 `C:\Users\PC\Desktop\k1-homepage` 에 그대로 있다.** 이 폴더는 관리자·동적 기능만 먼저 만든 것.
> 오픈 때 둘을 합친다(아래 §5).

---

## 1. 무엇인가

(주)케이원 홈페이지의 **관리자페이지**. 업체 담당자가 직접 글·사진·공고를 올리고 문의를 확인하는 화면.
회사 표준 `C:\Users\PC\Desktop\CLAUDE.md`(PHP 8.2 + CodeIgniter 4 + MySQL)를 그대로 따랐다.

- CodeIgniter **4.7.4**, PHP 8.4(로컬) / 8.2 이상(서버)
- 로컬 DB는 **SQLite**(`writable/k1.db`), 서버는 **MySQL**. 마이그레이션이 같아서 코드를 고칠 필요 없다.

## 2. 로컬에서 띄우는 법

```
php spark serve                 # 또는 Desktop\.claude\launch.json 의 k1-cms 사용
```
브라우저에서 `http://localhost:8080/admin/login`

- **최초 계정**: 비밀번호는 코드에 없다. `.env` 에 `admin.initialPassword` 를 적고
  `php spark db:seed AdminUserSeeder` 를 실행하면 만들어진다.
  ⚠️ **서버에 올린 뒤 관리자 화면에서 한 번 더 바꿀 것.**
- Composer 는 `C:\Users\PC\.tools\composer.phar` 에 있다(`php C:\Users\PC\.tools\composer.phar ...`).
- PHP 확장(intl·mysqli·pdo_mysql·pdo_sqlite·sqlite3·zip)을 php.ini 에서 켰다.
  원본 백업: `…\PHP.PHP.8.4_…\php.ini.bak-20260903`

## 3. 만들어 둔 것

| 화면 | 경로 | 기능 |
| --- | --- | --- |
| 로그인 | `/admin/login` | 세션 재발급, 아이디·비밀번호 구분 없이 같은 오류 문구 |
| 대시보드 | `/admin/dashboard` | 건수 4종 + 최근 게시글·문의 |
| 게시판 | `/admin/article` | 목록·검색·쓰기·수정·삭제, 공지 상단 고정 |
| 갤러리 | `/admin/gallery` | 사진 업로드·교체·삭제, 노출 순서 |
| 채용공고 | `/admin/recruit` | 등록·수정·삭제, 모집 기간, 노출 On/Off |
| 문의 내역 | `/admin/inquiry` | 목록·상세·삭제, 열면 읽음 처리, 메일 답장 |
| 비밀번호 변경 | `/admin/password` | 현재 비밀번호 확인 후 변경 |
| 관리자 계정 | `/admin/account` | 계정 추가·수정·삭제. 자기 자신·마지막 1개는 삭제 불가 |
| 회사 정보 | `/admin/setting` | 연락처·주소. 고치면 국문·영문 **18개 화면에 함께 반영** |

**표 6개**: `admin_users` `articles` `galleries` `recruits` `inquiries` `settings`
(모두 `created_at`/`updated_at`, 삭제 기능 있는 표는 `deleted_at` 소프트 딜리트)

## 4. 지킨 규칙 · 함정

- **CSRF·invalidchars 필터를 켰다.** CI4 기본값은 꺼져 있다 — 표준 3장 위반이라 `app/Config/Filters.php` 에서 활성화.
- **업로드 검사 3중**: 확장자 화이트리스트(jpg/png/webp) → 5MB 제한 → `getimagesize()` 로 진짜 이미지인지.
  파일명은 `getRandomName()` 으로 새로 만든다(한글·공백·확장자 위장 차단). 삭제는 업로드 폴더 안 경로만.
- **비밀번호는 `AdminUserModel::setPassword()` 로만 저장**한다(평문 저장 방지). 로그인 실패 시에도 해시 검증을 한 번 돌려 응답 시간으로 아이디 존재가 드러나지 않게 했다.
- **로그인 시도 제한**: 같은 IP 에서 1분에 6번까지. CI4 `throttler` 사용(`Auth::MAX_TRIES`).
- **업로드 사진은 자동으로 줄인다**: 원본 가로 1600px, 목록·타일용 축소본 640px(`thumb_` 접두).
  축소본이 없던 옛 사진은 `php spark db:seed ThumbBackfillSeeder` 로 채운다(여러 번 돌려도 안전).
- ⚠️ **SQLite 경로 함정**: `.env` 의 `database.default.database` 에 `writable/k1.db` 처럼 쓰면 안 된다.
  CI4 가 WRITEPATH 를 앞에 또 붙여 `writable/writable/…` 가 된다. **파일명만** 적을 것(`k1.db`).
- ⚠️ **정적 파일 함정**: `php -S … public/index.php` 로 띄우면 CSS·이미지가 안 나온다.
  라우터로 `vendor/codeigniter4/framework/system/rewrite.php` 를 써야 한다(launch.json 에 반영됨).
- ⚠️ **CSS 가 `hidden` 속성을 이긴다**: `display:block` 을 준 클래스에 `hidden` 을 달아도 보인다.
  `[hidden]{display:none !important}` 를 admin.css 에 넣어 뒀다.
- ⚠️ **CDP 캡처 403**: 헤드리스 크롬에 `--remote-allow-origins=*` 를 줘야 한다. 캡처 도구는 `_scratchpad/shot_admin.py`.

## 5. 오픈 때 할 일

1. 카페24 호스팅 개설(PHP 8.2+, MySQL). **`www` 상위 접근이 되는지 개발자에게 확인** — 안 되면 `public/` 내용물을 `www` 에 두고 `index.php` 경로 상수를 고쳐야 한다.
2. `.env` 를 MySQL 로 바꾸고 `php spark migrate`, `db:seed AdminUserSeeder`.
3. **정적 홈페이지 합치기** — `k1-homepage` 의 HTML·css·js·assets 를 이 폴더 `public/` 로 옮긴다.
   게시판·갤러리·채용공고 화면은 정적 HTML 대신 CI4 뷰로 바꾼다.
4. **문의 폼을 서버 발송으로** — 지금 `mailto:` 인 것을 `/api/contact` POST 로 바꾸고 `inquiries` 표에 저장 + 메일 발송.
5. `.htaccess`(ErrorDocument 404, https 강제), `writable/`·`public/uploads/` 쓰기 권한 775.
6. `CI_ENVIRONMENT = production` 으로 바꿀 것(개발용 디버그 툴바가 노출되지 않게).

## 6. 홈페이지(공개) 연결 — 2026-09-03 완료

관리자에서 올린 내용이 홈페이지에 나온다.

| 주소 | 뷰 | 내용 |
| --- | --- | --- |
| `/news.html` | `site/news.php` | 게시판 목록(공지 상단 고정·페이지 이동) + 갤러리 |
| `/news/view/N` | `site/news_view.php` | 게시글 상세(이전/다음 글) |
| `/recruit.html` | `site/recruit.php` | 채용안내 + 진행 중인 공고 |
| `/recruit/view/N` | `site/recruit_view.php` | 공고 상세(지원 메일) |

- 나머지 페이지(메인·회사소개·사업분야·프로젝트 실적·고객문의·처리방침·영문)는 **정적 파일 그대로**
  `public/` 에 복사해 두었다. 주소를 `.html` 로 유지해 기존 링크를 손대지 않았다.
- ⚠️ **`public/news.html`·`public/recruit.html` 파일을 만들지 말 것.** 파일이 있으면 서버가 그걸 먼저
  내보내서 CI4 뷰가 무시된다.
- ⚠️ **상세 화면은 주소가 한 단계 깊다**(`/news/view/1`). 상대 경로로 두면 CSS·이미지가 전부 깨진다.
  `news_view.php`·`recruit_view.php` 는 모든 경로를 `base_url()` 로 바꿔 뒀다.

### 두 폴더가 어긋나지 않게

같은 페이지가 `k1-homepage` 와 `k1-cms/public` 두 곳에 있다. 한쪽만 고치면 오픈 때 옛 내용이 나간다.

```
python _tools/check_sync.py          # 검사
python _tools/check_sync.py --pull   # 원본 내용으로 사본을 맞춤(한 방향, 원본은 안 건드림)
```

**업체 수정 요청을 반영한 뒤에는 반드시 이 검사를 돌릴 것.**

## 7. 회사 정보 · 문의 폼 — 2026-09-03 완료

### 회사 정보 (`settings` 표)

연락처·주소가 국문 9 + 영문 8 = **17개 파일에 박혀 있던 것**을 한 곳으로 모았다.
`/admin/setting` 에서 고치면 **18개 화면에 함께 반영**된다(대표 전화를 바꿔 전수 확인).

- 항목 정의는 `SettingModel::FIELDS` 한 곳. 여기 한 줄 더하면 관리자 화면도 같이 늘어난다.
- 뷰에서는 `$site['tel']` 처럼 쓴다. `BaseController` 가 전체 뷰에 공유한다.
- **비우면 그 줄이 화면에서 사라진다** — 울산 지사 전화·팩스가 지금 그 상태다(업체 미수령).
- ⚠️ 영문 주소는 로마자 표기라 설정에 넣지 않았다. 주소가 바뀌면 영문 뷰도 손봐야 한다.

### 정적 페이지를 CI4 화면으로 옮김

연락처를 넣으려면 페이지가 뷰여야 해서, `public/*.html` 을 `app/Views/site/pages/*.php` 로 옮겼다.
주소는 `.html` 그대로 유지해 기존 링크를 손대지 않았다(`Page` 컨트롤러 + 라우트).
`public/` 에는 이제 자산(css·js·assets·uploads)만 남는다.

### 문의 폼 서버 발송

`mailto:` → `POST /api/contact`. 접수하면 `inquiries` 표에 저장하고 담당자에게 메일을 보낸다.

- 화면을 새로 고치지 않고 그 자리에서 결과를 알려 준다(입력한 내용이 날아가지 않게).
- **메일 발송이 실패해도 접수는 성공**으로 본다 — 이미 DB 에 있고 관리자에서 볼 수 있으므로.
- 막아 둔 것: 개인정보 동의 필수 · 1시간 5건 제한 · 숨은 칸(honeypot)으로 자동 발송 차단.
- ⚠️ **CSRF 토큰은 요청마다 새로 발급된다.** 화면을 새로 고치지 않으므로 응답에 새 토큰을 실어 보내고
  스크립트가 갈아끼운다(`Contact::freshToken()` ↔ `v2.js`). 이걸 빼면 두 번째 전송이 403 이 된다.
- ⚠️ 서버에서 메일이 나가려면 `.env` 에 SMTP 설정이 필요하다(카페24 메일 또는 회사 메일 계정).
  지금은 로컬이라 발송이 되지 않고 로그만 남는다.

## 8. 아직 안 만든 것

- **개인정보 처리방침 값 관리** — 보유기간·수탁자·보호책임자·시행일.
- **프로젝트 실적 관리** — 실적 4건·고객사 로고 20개가 아직 뷰에 박혀 있다. 업체 의향 확인 필요.
- **게시글 첨부파일·본문 사진** — 지금 게시글은 글자만 된다. 업체 의향 확인 필요.
- 영문 게시판·갤러리·채용공고는 업체 결정으로 **하지 않는다**.

## 9. 미리보기

`python _preview/build_preview.py` — 로컬 서버에 로그인해 각 화면을 정적 HTML 로 떠서
`_preview/dist/` 를 만든다. 저장·삭제는 동작하지 않고 상단에 안내 띠가 붙는다.
⚠️ 업로드 폴더의 `.gitignore` 가 따라오면 **사진이 통째로 빠진다**(빌더가 지우게 해 뒀다).
2026-09-03 세영님 지시로 **업체 컨펌용 공개 배포는 하지 않기로** 함.
